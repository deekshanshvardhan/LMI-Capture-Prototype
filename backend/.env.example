# Supabase — used by the LMI capture API (REST client)
# Dashboard → Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Optional: SQLite for other prototype routes (products, etc.)
# DATABASE_URL=sqlite:///./expiry_prototype.db
